# Evidence

Source inventories and metadata only. Do not store parsed record content here.

Each row in `source-inventory.md` should contain: `source_id`, `source_type`, `date`, `account`, `segment`, `stage`, `quality`, `notes`.

To parse a source into a record, follow the runbook at `docs/architecture/parse-single-conversation-runbook.md`.
