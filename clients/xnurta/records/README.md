# Records

Parsed conversation records. One file per source.

To parse a new conversation, follow the runbook at `docs/architecture/parse-single-conversation-runbook.md`, then apply the Xnurta config pack in `clients/xnurta/config/` while coding the record.

File naming: `convrec-{source_id}.md`
