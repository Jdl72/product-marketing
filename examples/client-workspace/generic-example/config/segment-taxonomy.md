# Segment Taxonomy

## Segments

- `Segment ID`:
  - `Name`:
  - `Description`:
  - `Primary Buyer Persona`:
  - `Primary User Persona`:
  - `In-scope evidence types`:

## Notes

- Keep segment definitions specific enough to separate meaningfully different motions.
- Do not use "all customers" as a segment.
